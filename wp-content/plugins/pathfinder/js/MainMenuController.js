//instead of $, we must call jQuery, but within the ready() function with
//parameter $, we can use jQuery as we would expect
jQuery(document).ready(function($) {

    /* $(".characterLoadButton").click(function (e) {

     /*  $.ajax({
             type: 'POST',
             url: '../src/MainMenu/MainMenuHandler.php',
             data: {
                 'vorname': 'bar'
             },
             success: function(msg){
                 alert('wow' + msg);
             }
         });

        alert("test");

    });*/

});